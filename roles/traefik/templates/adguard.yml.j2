---
# {{ ansible_managed }}

http:
  routers:
    adguard:
      rule: "Host(`adguard.{{ root_domain }}`)"
      service: adguard
      tls:
        certResolver: http
      middlewares:
        - secure-headers
        - ip-whitelist
        - crowdsec

  services:
      adguard:
        loadBalancer:
          servers:
            - url: "http://{{ service_ip }}:3003"
