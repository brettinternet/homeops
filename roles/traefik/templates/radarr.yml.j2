---
# {{ ansible_managed }}

http:
  routers:
    radarr:
      rule: "Host(`radarr.{{ root_domain }}`)"
      service: radarr
      tls:
        certResolver: http
      middlewares:
        - secure-headers
        - ip-whitelist
        - crowdsec
        - oauth

  services:
      radarr:
        loadBalancer:
          servers:
            - url: "http://{{ service_ip }}:7878"
