---
- hosts:
    # - master
    # - worker
    - pi-2
    - pi-3
    - harambe
    - archie
  gather_facts: false
  become: true
  tasks:
    - name: Reboot
      ansible.builtin.reboot:
      retries: 10
      delay: 10
