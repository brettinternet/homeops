---
- hosts:
    - master
    - worker
  become: true
  roles:
    - os
    # - clamav
    # - role: cockpit
    #   when: ansible_facts['distribution'] | lower != 'archlinux'
    # - role: zfs
    #   when: ansible_facts['distribution'] | lower != 'archlinux'
