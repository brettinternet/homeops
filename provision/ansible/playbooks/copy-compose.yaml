---
- hosts:
    - docker
  gather_facts: false
  tasks:
    - name: Copy docker files
      ansible.builtin.copy:
        src: "{{ playbook_dir }}/../../docker"
        dest: "{{ ansible_user['home'] }}/compose"
        mode: "0644"
