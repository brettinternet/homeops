---
- name: Install packages
  ansible.builtin.package:
    state: present
    name:
      - docker
      - python-docker

- name: Setup proxy services
  ansible.builtin.include_tasks: proxy.yaml

- name: Setup proxy services
  ansible.builtin.include_tasks: proxy.yaml
