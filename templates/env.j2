# {{ ansible_managed }}
# Values defined in vars/secrets.yml

PUID={{ puid | default("{{ ansible_facts['user_uid'] }}") }}
PGID={{ pgid | default("{{ ansible_facts['user_gid'] }}") }}

TZ={{ tz }}

CLOUDFLARE_EMAIL={{ cloudflare_email }}
CLOUDFLARE_API_KEY={{ cloudflare_api_key }}
ROOT_DOMAIN={{ root_domain }}

APPDATA_DIR={{ appdata_dir }}
STORAGE_DIR={{ storage_dir }}

NETWORK_BIND_ADDR={{ network_bind_addr }}
HOMELAB_LOCAL_IP={{ homelab_local_ip }}

HTPASSWD={{ htpasswd }}

DOCKER_PGID={{ docker_pgid }}

OAUTH_CLIENT_ID={{ oauth_client_id }}
OAUTH_CLIENT_SECRET={{ oauth_client_secret }}
OAUTH_SECRET={{ oauth_secret }}
OAUTH_WHITELIST={{ oauth_whitelist }}

PLEX_CLAIM_CODE={{ plex_claim_code }}
PLEX_HOSTNAME={{ plex_hostname }}
PLEX_ALLOWED_NETWORKS={{ plex_allowed_networks }}

MINIFLUX_PASSWORD={{ miniflux_password }}
MINIFLUX_POSTGRES_PASSWORD={{ miniflux_postgres_password }}
